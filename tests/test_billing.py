"""Billing / subscription router tests — REMOVED.

The billing router (app.routers.billing_router) and PlanService
(app.services.plan_service) have been deleted.  This file is kept as
a placeholder so the test suite still discovers it, but all tests have
been removed.
"""
